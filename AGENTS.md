Read `~/configs/GLOBAL-AGENTS.md` (fallback: https://raw.githubusercontent.com/justinmoon/configs/master/GLOBAL-AGENTS.md). Skip if both unavailable.

# Agent Notes

Do not add workspace guardrails, root-folder edit restrictions, project-folder enforcement, or similar policy layers unless Justin explicitly asks for them.

Keep this repo lean. Prefer small, visible changes that improve the working coding-agent loop. Avoid building extra safety frameworks around the workspace.

Use `just --list` for current development and deploy commands.
